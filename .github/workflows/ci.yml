name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt
          pip install pytest pytest-asyncio

      - name: Run pytest
        env:
          PYTHONPATH: ${{ github.workspace }}
          # CI placeholders only — supabase-py never connects during unit tests.
          # .invalid is reserved (RFC 2606) and cannot resolve.
          SUPABASE_URL: https://ci-placeholder.invalid
          SUPABASE_SERVICE_KEY: ci-placeholder-not-a-real-key
        run: python -m pytest tests/ -v

      - name: Dependency audit (warn-only)
        run: |
          pip install pip-audit
          pip-audit -r backend/requirements.txt || true

  frontend:
    name: Frontend lint + build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        env:
          # CI placeholders only — never hit at build time.
          # .invalid is reserved (RFC 2606) and cannot resolve.
          NEXT_PUBLIC_SUPABASE_URL: https://ci-placeholder.invalid
          NEXT_PUBLIC_SUPABASE_ANON_KEY: ci-placeholder-not-a-real-key
          NEXT_PUBLIC_API_URL: http://localhost:8000
        run: npm run build
